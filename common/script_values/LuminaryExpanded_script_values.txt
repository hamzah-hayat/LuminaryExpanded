# Script Values for Luminary Expanded
le_doctor_workforce = {
	base = 0
	complex_trigger_modifier = {
		trigger = num_assigned_jobs
		parameters = {
			job = healthcare
		}
		mode = add
	}
	divide = 100		# Renormalise to "per 100 jobs"
	floor = yes
	multiply = 0.1 # 100 Doctors gives 0.1 progress per month
}

le_biologist_workforce = {
	base = 0
	complex_trigger_modifier = {
		trigger = num_assigned_jobs
		parameters = {
			job = biologist
		}
		mode = add
	}
	divide = 100		# Renormalise to "per 100 jobs"
	floor = yes
	multiply = 0.1
}

# Replacement for removed vanilla paragon_origin_count_egalitarians
# Counts the number of egalitarian pops in the country for decision cost scaling
le_count_egalitarians = {
	base = 0
	complex_trigger_modifier = {
		trigger = pop_ethic_amount
		parameters = {
			ethic = ethic_egalitarian
		}
		mode = add
	}
}
